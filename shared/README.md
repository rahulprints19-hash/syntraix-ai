# Shared

Use this folder for contracts that multiple runtimes need to understand:

- shared TypeScript types
- JSON schemas
- prompt templates
- agent tool manifests
- API request and response contracts
